# CLAUDE.md（3rd Stage 公式LP）

ルートの `Desktop/Claude/CLAUDE.md`（全プロジェクト共通テンプレート）を継承する。
本ファイルはこのプロジェクト固有の決定事項のみを記録する。

## 変更履歴

| 日付 | 変更内容 |
|---|---|
| 2026-09-10 | ユーザーから正式なSNS実URLの提供を受け、残っていた2件のブロッカーを解消。①CTA6箇所＋追従バーのLINEリンクを暫定URL（`https://line.me/R/ti/p/%40289wzocw`）から公式の`https://lin.ee/VlXBZzh`に差し替え、②フッターSNSアイコン4件（`href="#"`のまま保留していたX/Instagram/YouTube/TikTok）を実URLに接続（X: `https://x.com/3rdstagepr`、Instagram: `https://www.instagram.com/3rdstage.pr/`、YouTube: `https://www.youtube.com/@3rdStagepr`、TikTok: `https://www.tiktok.com/@3rd.stageai`）。ローカルプレビューでリンク先を実測確認済み |
| 2026-09-10 | 「最速で公開」を目的に、残る公開ブロッカーを解消。ユーザーへの逆質問3件（①privacy/tokushoho・②素材待ちセクションの扱い・③公開先）で方針を確定した上で対応。**①法的ページを新規作成**：`privacy.html`（プライバシーポリシー。フォーム非設置・LINE経由の相談のみ・現時点でCookie/解析ツール不使用の旨を明記）、`tokushoho.html`（特定商取引法に基づく表記。所在地は「請求があれば遅滞なく開示」、責任者名は西村憧平として記載。**受注制作が特商法上の「通信販売」に該当するかは未確認のまま、ユーザーの了承のもと最低限の内容で作成した暫定版**であり、正式な法務確認は別途推奨）。フッターの2リンクのTODOコメントを解消。**②素材待ちの4カテゴリを非表示化**：導入事例(`#case`)ブロックと制作実績(`#works`)セクションは丸ごと`<template>`タグで囲んで非表示化（中身のコード・TODOコメントはそのまま保持。素材が揃ったら`<template>`と閉じタグを削除するだけで復帰できる設計。`#works`を指す3箇所のナビリンク（PCナビ・モバイルメニュー・フッターナビ）もコメントアウトで無効化）。信頼バーの実数字2セル（制作アプリ数・導入店舗数）はコメントアウトし、確定済みの2セルのみ`trust-grid--2`で中央寄せ2カラム表示に変更。メンバー写真2名分のプレースホルダー（`[写真]`表示）は削除しテキストのみに（`member-body`は元々`flex:1`のためCSS変更不要）。あわせてFVの「1年分でまる5日分」「1年に5時間」「15分」など未確定の具体的数字（`仮`マーク付き）は、視覚的にも見苦しく数字の裏取りもできていなかったため、数字を出さない言い回しに修正。FVスクリーンショットのプレースホルダーは④できることセクションと同じ実画面（`app-invoice.png`）を暫定流用する形で解消（専用の上部切り出し画像は撮影後に差し替え予定、コメントに記載）。**③公開作業の分担を確認**：GitHubへの公開はこのセッションで対応し、Netlify Dropでの実際のデプロイはユーザー自身が行う方針。`https://github.com/speciale0517/3rdstage-official-lp`（Public）にpush済み。**なお`wireframe.html`には非公開方針の人物名（なおき）の記載が残っており、Publicリポジトリに含めると漏洩するため、`.gitignore`で`wireframe.html`・`design-directions/`をリポジトリから除外した**（ローカルには残したまま）。**残タスク**：③ミエルカ統計画面・制作実績サムネイル4点・導入事例の実物写真とお客様の声・メンバー写真2名分・信頼バーの実数字は引き続き未着手（`<template>`または該当コメントとして保持）、公式LINE友だち追加のiPhone実機タップ確認、tokushoho.htmlの内容の正式な法務確認 |
| 2026-09-04 | フェーズ4（実装）着手。設計書（`3rdStage_LPワイヤーフレーム_20260904`）とdesign canvas採用案（B案）に基づき、複数ファイル静的サイトとして`index.html`・`assets/css/base.css`・`assets/css/style.css`・`assets/js/main.js`を実装。全14ブロックを1ページに実装済み |
| 2026-09-04 | ③お困りごとと④できることの間に「3.5 ビフォーアフター」ブロックを新規追加（IG投稿用に制作済みのイラスト2点を`assets/img/illust-tegaki.png`・`illust-sumaho.png`として流用。画像上端に生成時の黒い線状ノイズがあったためPillowでピクセル修正済み）。SPでも横並びを維持する必要があったため、#caseの`.case-flex`（SPで縦積みになる仕様）は流用せず`.ba-flex`を新設。あわせて氏名を漢字フルネーム＋ふりがな括弧書きに変更（西村直輝／西村憧平／福田雄翔）、なおきの役職を「営業担当」から「代表」に統一（従来の代表プレースホルダーと統合）、フッターの法人名・連絡先を確定値に更新 |
| 2026-09-04 | ビフォーアフターブロックを修正。①`illust-tegaki.png`左端にも黒い線ノイズが残っていたため追加修正、②`.ba-flex`の列幅を`1fr auto 1fr`から`auto auto auto`＋`justify-content:center`に変更（1frだと画像が離れて見えるため画像同士を近づけた）、③矢印が「これまで／いまは」ラベル行に浮いていたため、ラベル行と画像行の2行構成に組み直して矢印を画像の高さに揃えた、④「計算も、書き直しも、もう要りません。」を16px→32px（PC 40px）に拡大。あわせて最終CTAのQRプレースホルダーを実際のLINE公式アカウントQR画像（`assets/img/qr-line.png`）に差し替え |
| 2026-09-04 | 「④3rd Stageができること」①②のプレースホルダーを、実際のアプリ画面スクリーンショット（ダミーデータ）に差し替え。iPhone実機風に見せるCSSコンポーネント`.device-frame`（紺の縁取り＋Dynamic Island風の切り欠き）を新設し、①請求書作成画面は1枚、②お客様検索/車両情報は2枚並べて表示。使用画像は`assets/img/app-invoice.png`・`app-customer-search.png`・`app-vehicle.png`（実機スクリーンショットを640px幅にリサイズ）。**初回受領画像はアプリ名「ケンズ請求書アプリ」が写り込んだままだったため使用を見送り、本人が該当箇所を「○○○請求書アプリ」に隠して再送したものに差し替えて反映した**（先方名を伏せる意図のため、以後同種の画像を扱う際はアプリ名等の写り込みを都度確認すること）。③（ミエルカ統計画面）・FVクロップ・導入事例セクションは対象外のまま |
| 2026-09-09 | クロスレビューにあわせて⑪私たちについてを2名体制に変更。西村直輝のメンバーブロックを削除（一言コピーも破棄）し、西村憧平の役職を「開発担当」→「**社長 兼 開発担当**」に変更。あわせてフッター会社概要から「所在地」の行を削除した（法人名・連絡先は据え置き）。**所在地を非公開にしたため、特商法表記（`tokushoho.html`）では所在地の記載義務との整合を別途検討すること**（6章参照） |
| 2026-09-10 | クロスレビュー指摘21件（コード不備10件＋UI/UX改善11件）のうち、実材料が揃っている項目を一括修正。**主な変更**：①CTA6箇所＋追従バーのLINEリンクを`@289wzocw`から導出した公式URL（`https://line.me/R/ti/p/%40289wzocw`）に接続（実URLは未確認のためLINE公式アカウントマネージャーでの確認・実機タップ確認が残作業）、②PCでのアンカー詰まり（`:root`の`--header-h`をPCメディアクエリ内で直接上書き）、③追従CTAバーがフッター著作権表記を覆う問題（`footer`の`padding-bottom`に`--follow-h`を加算、追従バー自体も2行表記化に伴い56px化し`--follow-h`を64→76pxへ更新）、④`<footer>`を`<main>`の外に移動、⑤モバイルメニューの背面スクロールロック・`aria-expanded`/`aria-hidden`・Escキー・簡易フォーカストラップを追加、⑥タップ領域44px未満だったフッターナビ・フッター法的リンク・SNSアイコン・ヘッダーロゴを44px以上に拡大、⑦本文サブカラー`--color-text-sub`を`#777770`→`#5f5f57`に変更しコントラスト比4.5:1以上を確保、⑧favicon/apple-touch-icon/OGP画像を専用書き出し（`favicon-32.png`・`apple-touch-icon-180.png`・`og-image.jpg`）に差し替え、⑨全`<img>`に`width`/`height`と`loading="lazy"`を付与、画像を最適化（`illust-tegaki.png` 226KB→25KB、`illust-sumaho.png` 144KB→27KB、`logo-icon.png` 152KB→42KB）、⑩Google Fontsの未使用ウェイト（900）を削除、⑪フッターSNSアイコンをテキスト略記（X/IG/YT/TT）からインラインSVG＋視覚的に隠したラベルに変更、⑫FAQPageのJSON-LD構造化データを追加、⑬FVキッカーを「熊本・福岡の、小さなお店へ」→「熊本・福岡｜お店に合わせてつくる業務アプリ」に変更（業態が伝わるまでが遅い問題への対処）、⑭選ばれる理由④を「熊本・福岡の会社です。」（断定）→「熊本・福岡へ、顔を出して伺います。」（行動の約束）に変更（本人確認により福岡に実拠点はあるが、所在地非公開の方針と両立する表現に統一）、⑮メンバー名のふりがな部分を`.member-kana`として分離し375px幅での不自然な折り返しを解消、`.member-role`のフォントサイズ・コントラストを改善、⑯本文が14px以下だった多数の要素を15〜16pxに引き上げ、⑰お客様の声ブロックの偽斜体（`font-style:italic`）を解除、⑱`<title>`を短縮し「アプリ」を前方に配置、⑲viewportから`maximum-scale=1, user-scalable=no`を削除しピンチズームを許可（本人判断。ルートCLAUDE.md 4章は業務アプリ向け規約のためLPはその例外とする）、⑳インライン`style`属性2箇所をCSSクラス化、㉑`.final-qr .placeholder-box`の死にルールを削除。**デプロイ対象の切り分け**（A-9）：`wireframe.html`・`design-directions/`を誤って公開しないよう、本番公開対象（`index.html`・`assets/`）を新設した`site/`サブディレクトリへ移動。実際のホスティングでは`site/`をデプロイのベース（公開ルート）に指定すること。ローカル確認用の`.claude/launch.json`（`3rdstage-lp`）も`site/`を向くよう更新済み。**保留**：SNS4アカウント（X/IG/YT/TikTok）の実URLが未確定のため`href="#"`のまま、導入事例(#case)は本人判断でBefore/After構成を維持し素材（実物写真・実画面）待ち、信頼バーの実数字・③ミエルカ統計画面・実績サムネイル5点・メンバー写真2名分は引き続き未着手 |

## 1. プロジェクト概要

- プロジェクト名: 3rd Stage 公式LP
- 対象ユーザー: 熊本・福岡の小さなお店（整備工場等）の店主・スタッフ（50〜70代想定）
- 目的: 3rd Stageの業務アプリ制作サービスをLINE相談へ導線する公式ランディングページ
- 提供形態: 複数ファイルの静的サイト（`site/index.html` + `site/assets/css` + `site/assets/js`）。localStorage不使用のためキー命名プレフィックス（ルート3.1章）は対象外
- ディレクトリ構成：`site/`＝本番公開対象（デプロイ時はここをベースディレクトリに指定する）。`wireframe.html`・`design-directions/`はフェーズ2〜3の検討過程の記録であり、`site/`の外に置いて公開対象から除外している（6章参照）

## 2. このプロジェクト固有の配色（ルート2章・5章の例外）

**3rd Stage全体のブランドカラー（紺 `#1A1A6E` / 生成り白 `#FBFAF6`）とは別に、LP専用で以下を使用する。**

| 項目 | 値 | 備考 |
|---|---|---|
| 紺（LP専用） | `#16233F` | ページ全体の紺はこちら |
| 生成り白（LP専用） | `#F0ECE2` | ページの地色 |
| ロゴ画像自体の紺 | `#1A1A6E`（原本のまま） | ロゴPNG（`assets/img/logo-icon.png`等）は加工していないため、ヘッダー等でページの紺と2色が並ぶ。**意図的な選択であり実装ミスではない** |

適用範囲はLPのみ。SNS・名刺等の既存ブランド仕様はこのプロジェクトの変更対象外。

## 3. 余白・書体・部品の規則（design canvas B案を全14ブロックに適用）

| トークン | 値 |
|---|---|
| セクション余白（標準） | 44px（`--space-section`） |
| 見出し(H1) | 27px/行間1.5（PC 34px） |
| 本文 | 16px/行間1.8 |
| ボタン高さ | 56px |
| 角丸 | 8px（`--radius`） |
| ブレークポイント | 768pxの1点のみ |
| 書体 | Zen Maru Gothic（Google Fonts） |

信頼バー（22px）・中間CTA（32px）・最終CTA（48px）は帯の性質上、標準値から意図的に調整している。

## 4. ヘッダーロゴの扱い

`assets/img/logo-icon.png`（アイコン28px切り出し・透過）+ Zen Maru Gothicで「3rd Stage」文字を併記。正方形フルロゴ（`assets/img/logo.png`）から書き出した`favicon-32.png`・`apple-touch-icon-180.png`・`og-image.jpg`（紺背景に中央配置、1200×630）をファビコン・OGP画像に使用する。

## 5. プレースホルダ（`【仮】`・`<!-- TODO:仮 -->`）について

未確定情報（実測数字・店名・代表者名・メンバー写真・実画面スクショ等）は、差し替え漏れ防止のため`<span class="todo">仮</span>`表示とHTMLコメントで残したまま実装している。差し替え時はコメントを検索して該当箇所を確認すること。

**2026-09-10、「最速で公開」のため、素材/許諾が揃っていない箇所は「仮」表示のまま公開せず、以下の方針で非表示化した。**

- **ブロックごと非表示にした箇所**：導入事例(`#case`)・制作実績(`#works`)は、内容ごと`<template data-hidden-section="...">`タグで囲んで無効化した（`<template>`の中身はブラウザに描画されないため実装済みのコードをそのまま保持でき、素材が揃ったら`<template>`と閉じタグを削除するだけで復帰できる）。`#works`を指すナビリンク（PCナビ・モバイルメニュー・フッターナビの3箇所）も合わせてコメントアウトしてある
- **一部要素だけ非表示にした箇所**：信頼バーの実数字2セル（制作アプリ数・導入店舗数）はHTMLコメントで無効化し、確定済みの2セルのみ`.trust-grid--2`で中央寄せ2カラム表示に変更。メンバー写真2名分の`[写真]`プレースホルダーは削除しテキストのみ表示（`member-body`は元々`flex:1`のためレイアウト調整は不要だった）。③3rd Stageができることのミエルカ統計画面プレースホルダーも同様にHTMLコメントで無効化
- **数字を伴う「仮」コピーは、非表示にせず数字を出さない言い回しに書き換えた**：FVの「1年分でまる5日分」、お困りごとカードの「1年に5時間」「15分」は、裏取りできていない数字を公開するのは避けたいがブロックごと消すと文脈が崩れるため、数字なしの表現に修正済み（実測値が確定したら具体的な数字入りに戻すのも可）
- **FVのスクリーンショットプレースホルダーは、④できることセクションと同じ実画面（`assets/img/app-invoice.png`）を暫定流用して解消**。本来は「請求書画面 上部を切り出し＋拡大」の専用ショットに差し替える想定（コードにコメントで記載）

## 6. 公開前のブロッカー（2026-09-10時点）

- ~~`privacy.html`・`tokushoho.html`が未作成~~ → 2026-09-10、最低限の内容で両ページを新規作成し解消。所在地は「請求があれば遅滞なく開示」、責任者名は西村憧平として記載（5章参照）。**ただし対面商談前提のため特商法上の「通信販売」に該当するかを含め、正式な法務確認は未実施の暫定版**
- ドメイン・ホスティング先：ユーザーがGitHub経由でNetlify Dropに自分でデプロイする方針（2026-09-10決定）。独自ドメインは未定
- 会社概要のうちメンバー写真・掲載許諾（店名／お客様の声）は法人側の確認待ち（法人名・連絡先は2026-09-04に確定済み）。**2026-09-10、写真プレースホルダーは非表示化済み（5章参照）**
- ~~公式LINEの友だち追加URLが未確認~~ → 2026-09-10、正式URL`https://lin.ee/VlXBZzh`を反映し解消。**iPhone実機タップでの友だち追加画面遷移確認のみ残作業**
- ~~SNS4アカウント（X/Instagram/YouTube/TikTok）の実URLが未確定~~ → 2026-09-10、実URLを反映し解消
- **2026-09-10、以下は素材が揃うまで非表示化**（5章参照）：信頼バーの実数字（制作アプリ数・導入店舗数）／③3rd Stageができることのミエルカ統計画面／導入事例(`#case`)ブロック全体／制作実績(`#works`)セクション全体。いずれもコードは保持したまま非表示にしているため、素材が揃い次第復帰できる

## 7. デプロイ対象の切り分け（2026-09-10決定）

`wireframe.html`・`design-directions/`（検討過程の記録。`wireframe.html`には非公開方針の「なおき」の記述が残る）を誤って公開しないよう、本番公開対象を`site/`サブディレクトリに分離した。

- `site/index.html`・`site/assets/` … 本番公開対象。**実際のホスティング先ではこの`site/`をデプロイのベースディレクトリに指定すること**
- プロジェクト直下の`wireframe.html`・`design-directions/`・`CLAUDE.md` … 検討過程・運用ドキュメント。デプロイ対象に含めない

## 8. ローカル確認方法

ブラウザで直接 `site/index.html` を開くと相対パスのCSS/JSが正しく読み込まれない環境がある。プロジェクトフォルダ直下でローカルサーバーを立てて確認すること。

```bash
cd "3rdStage公式LP/site"
python3 -m http.server 8770
```

`.claude/launch.json`（`Desktop/Claude`直下）の`3rdstage-lp`設定は`site/`を向くよう登録済み。
